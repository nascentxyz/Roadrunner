// * Import styles
import queryStyles from "./QueryList.module.css";
import footerStyles from "./Footer.module.css";

// * Redefine expoert vards
const grid = queryStyles.grid;
const card = queryStyles.card;
const footer = footerStyles.footer;
const logo = footerStyles.logo;

// * EXPORT!!
export {
    grid,
    card,
    footer,
    logo
}